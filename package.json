{
  "name": "face-gender-detector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=react-jsx --platform=browser --format=esm --define:process.env.API_KEY=\\\"$API_KEY\\\" --external:react --external:react-dom --external:@google/genai"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}